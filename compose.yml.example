version: '3'
services:
  app:
    tty: true
    stdin_open: true
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    image: apollo-blog
    ports:
      - "127.0.0.1:4000:4000"
    volumes:
      - ./:/app
